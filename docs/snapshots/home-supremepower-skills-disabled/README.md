# Snapshot: `~/.supremepower/skills/disabled`

Read-only copy of skills moved under **`disabled/`** in the Gemini custom skills home (`~/.supremepower/skills/`). Copied for reference in-repo; **not** loaded by the extension from this path.

**Not copied:** `~/.supremepower/config.json`, `logs/` (may contain machine-specific or sensitive paths).
