# Imported Qwen Markdown (read-only mirror)

**Source:** Copies were taken from `~/.qwen/*.md`, `~/.qwen/docs/*.md`, and `~/.qwen/docs/plans/*.md` on the machine that hosts this clone.

**Originals:** Nothing under `~/.qwen` was modified by this import. Edits and path rewrites exist **only** under `/Users/steven/supremepowers/docs/imported-qwen/`.

**Path rewrites (in copies only):**

- `~/iterm2/superpowers/...` → `/Users/steven/supremepowers/...` where that matches this repository layout.
- `~/.qwen/<top-level-imported-md>` and `~/.qwen/docs/...` → `/Users/steven/supremepowers/docs/imported-qwen/...` so cross-links resolve inside this tree.

**Unchanged on purpose:** Paths that refer to live Qwen runtime (`~/.qwen/superpowers/`, `~/.qwen/skills/`, `~/.qwen/settings.json`, `~/.supremepower/`, etc.) are left as-is because they are **not** stored in this repo.

To refresh this mirror, re-copy from `~/.qwen` and re-run the path rewrite step in your tooling (or ask to regenerate).
